/**
 * 지도 오버레이 z-index.
 *
 * 파일마다 흩어 두면 서로를 가리는 사고가 반복된다(선택 카드가 내 위치 점 아래로
 * 깔리는 버그가 실제로 있었다). 새 오버레이를 만들면 반드시 여기에 자리를 잡고 시작한다.
 *
 * 주변 핀과 게시글 핀은 동시에 뜨지 않는다 — 게시글이 포커스되면 호출부가 주변 목록을
 * 비운다(이 전제는 `MapPanel`의 `nearbyEnabled`가 보장한다). 그래서 선택된 주변 핀을
 * 게시글 핀보다 위에 둬도 서로 가릴 일이 없다. 소비자가 늘어나면 이 전제부터 다시 본다.
 */
export const MAP_Z = {
  /** 주변 장소 핀(기본) */
  nearbyPin: 1,
  /** 내 위치 파란 점 */
  myLocation: 2,
  /** 게시글 번호 핀 */
  postPin: 3,
  /** 선택된 주변 장소 핀 — 카드가 무엇보다 위에 떠야 한다 */
  nearbyPinSelected: 4,
} as const;
